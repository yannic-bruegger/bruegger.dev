# Deployment Configuration
# Copy this file to .env and fill in your server details

# Server Configuration
REMOTE_HOST=srv1011411
REMOTE_USER=root
REMOTE_PRODUCTION_PATH=/var/www/bruegger.dev
REMOTE_STAGING_PATH=/var/www/bruegger.dev-staging

# SSH Configuration (optional - use SSH keys instead)
# SSH_KEY_PATH=~/.ssh/id_rsa

# Optional: Backup before deployment
BACKUP_ENABLED=true
BACKUP_PATH=/var/www/backups